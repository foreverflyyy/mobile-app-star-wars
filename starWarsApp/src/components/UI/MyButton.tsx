import React from 'react';
import {Button} from 'react-native';

const MyButton = () => {
    return (
        <Button
            title={"Button"}
            onPress={() => {}}
        />
    );
};

export default MyButton;
